function someFn(param = 0) {
  if (param === 0) {
    return 0;
  }

  if (param === 1) {
    return 1;
  }

  if (param === 2) {
    return 2;
  }

  if (param === 3) {
    return 3;
  }
}

export default someFn;